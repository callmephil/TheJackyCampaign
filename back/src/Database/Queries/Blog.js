import PreparedStatement from './PreparedStatement';

const initializeBlog = async (Database) => {
    try {
        const controller = {
        }

        return controller
    } catch (e) {
        console.log(`initializeBlog ${e}`)
    }
}


export default initializeBlog